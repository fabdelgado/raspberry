version: '3.3'
services:
  duckdns:
    image: linuxserver/duckdns
    container_name: duckdns
    environment:
     - TZ=America/Montevideo
     - SUBDOMAINS=${SUBDOMAIN}
     - TOKEN=${TOKEN}
     - LOG_FILE=false
    volumes:
      - './config:/config'
    restart: always
